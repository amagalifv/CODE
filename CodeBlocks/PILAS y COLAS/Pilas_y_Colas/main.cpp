# include<iostream>
# include<cstdlib>
# include<cstring>
using namespace std;

#include "clsCola.h"
#include "clsPila.h"


int main(){
    /*
    // -----PARA COLA------
    Cola objCola(5);
    int num, num2;

    cout<<"NUMERO: ";
    cin>>num;

    while (objCola.Agregar(num)) {
        cout<<"NUMERO: ";
        cin>>num;
    }
    while (objCola.Sacar(num2)) {
        cout<<endl;
    }

    // -----PARA PILA------
    Pila objPila(5);
    int num3,num4;

    cout<<"NUMERO: ";
    cin>>num3;

    while (objPila.Agregar(num3)) {
        cout<<"NUMERO: ";
        cin>>num3;
    }
    while (objPila.Sacar(num4)) {
        cout<<endl;
    }
    */


    system("pause");
    return 0;
}
